[project]
name = "desync-data-processing-extension"
version = "0.1.0"
description = "Y Combinator website scraper using Desync AI bulk search API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "desync_search",
]
